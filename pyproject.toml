[project]
name = "roast-my-pitch"
version = "0.1.0"
description = "A local LLM-powered hackathon idea roaster."
requires-python = ">=3.9"
dependencies = [
    "flask>=3.0,<4",
    "requests>=2.31,<3",
]

[tool.uv]
package = false
